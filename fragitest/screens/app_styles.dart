import 'package:flutter/material.dart';

class AppStyles {
  // Colors
  static const Color blueColor = Color(0xFF003366);
  static const Color backgroundColor = Color(0xFF003366);

  // Text Styles
  static const TextStyle screenTitle = TextStyle(
    fontFamily: 'Caesar Dressing',
    fontSize: 47,
    color: Colors.white,
  );

  static const TextStyle nameOfPoint = TextStyle(
    fontFamily: 'Caesar Dressing',
    fontSize: 34,
    color: Colors.white,
  );

  static const TextStyle descriptionOfPoint = TextStyle(
    fontFamily: 'Finlandica',
    fontSize: 16,
    color: Colors.white,
  );
}
